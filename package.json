{
  "name": "midnight-shuto",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vite",
    "build": "npm run lint && npm run typecheck && vite build --configLoader runner",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit -p tsconfig.app.json --pretty false && tsc --noEmit -p tsconfig.node.json --pretty false && tsc --noEmit -p tsconfig.sw.json --pretty false && tsc --noEmit -p tsconfig.scripts.json --pretty false",
    "lint": "eslint .",
    "test:quality": "npm run typecheck && npm run lint",
    "test:e2e": "node scripts/e2e.ts",
    "test:performance": "node scripts/performance-e2e.ts",
    "test:visual": "node scripts/visual-e2e.ts",
    "models:export": "node scripts/export-models.ts"
  },
  "dependencies": {
    "cannon-es": "^0.20.0",
    "three": "^0.181.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.4.0",
    "@types/three": "^0.181.0",
    "eslint": "^10.9.1",
    "jiti": "^2.7.0",
    "playwright-core": "^1.62.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.68.0",
    "vite": "^7.1.12"
  }
}
